# Pinned, mutually-compatible dependency set (Python 3.11+).
# Install with:  pip install -r requirements.txt
#
# LangChain is the primary orchestration framework (LCEL Runnables).
# OpenAI SDK is pulled in transitively by langchain-openai and used for the model calls.

langchain==0.3.27
langchain-core==0.3.75
langchain-openai==0.3.33
openai==1.109.1
pydantic==2.11.9
tenacity==9.1.2
python-dotenv==1.1.1
